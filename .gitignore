node_modules/
dist/
*.tgz
.DS_Store
*.log
coverage/
.env
.env.local
# Test-fixture .env files contain only placeholder credentials and MUST be
# tracked — integration tests in CI fail to build their Docker images
# without them. See testing/fixtures/ironclaw/{insecure,secure}/.env.
!testing/fixtures/**/.env
devnotes/
prds/
feed-signing-key.pem
CLAUDE.md
internal_data/
probe/dist/
probe/probe
probe/vaso-probe
bin/vis/
vaso-visualizations/
.claude/
# Internal full-scanning test kit — local/testing only, never published
vaso_full_scanning_testing/
